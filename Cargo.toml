[workspace]
members = [
    "daily",
    "windowsperson",
    "windowsservicemanger",
    "shared",
]
resolver = "2"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
strip = true
opt-level = "z" 
